Функции могут принимать аргументы по умолчанию:
```
def hello_world(hello=True):  
    if hello:  
        return "Hello World!"  
    else:  
        pass  
  
  
a = hello_world()  
  
print(a)
```
