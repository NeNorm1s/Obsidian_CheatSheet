Функции могут принимать значение атрибутов без записи их через вызов:
```
def hello_world(hello=True):  
    if hello:  
        return "Hello World!"  
    else:  
        pass  
  
  
a = hello_world()  
  
print(a)
```
